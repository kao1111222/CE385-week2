ex1-profile
const ใช้สร้างตัวแปรเก็บข้อมูลส่วนตัว และไม่ใช้ var ตามเงื่อนไข
nickname, studentid, age, major, registeredCourse ใช้เก็บข้อมูล 5 อย่างตามโจทย์
remainingYears เก็บจำนวนปีที่เหลือ เพื่อใช้คำนวณปีที่จะจบ
console.log() ใช้แสดงข้อมูลออกทาง Console
` ` คือ Template Literal ใช้สำหรับแทรกตัวแปรด้วย ${}
${2569 + remainingYears} ใช้คำนวณปีที่จะจบ โดยไม่พิมพ์เลขตายตัว
// คือ Comment ซึ่งในโค้ดมีมากกว่า 2 บรรทัดตามเงื่อนไข

ex2-types

